# Session 2026-05-14 #01

**Agent:** Codex
**Phase:** Mobile takeover / stabilization
**Duration:** approx 4 hours

## What got done
- Read AGENTS.md, the mobile spec, master plan, Phase 2 plan, latest session log, parity audit, billing/launch plans.
- Audited current repo state: code is ahead of old checkbox plans; mobile has real auth/onboarding/chat/settings/memory/pricing/push/billing scaffolding.
- Installed dependencies and restored verification commands.
- Fixed mobile lint failures across auth, settings, chat, onboarding, and pricing.
- Fixed stale web fast-test failures in `chat-arrival`, `squad-persistence`, and `system-prompt`.
- Added `apps/mobile/lib/config.ts` and routed mobile API calls through `app.json > extra.apiBaseUrl`.
- Removed noisy mobile auth/onboarding debug logs.
- Wrote takeover plan: `docs/superpowers/plans/2026-05-14-codex-mobile-takeover-plan.md`.
- Fixed the Android Expo onboarding loop by making the local auth profile update resilient when an old saved session has no loaded profile row.
- Fixed the completed-user “Retake Vibe Quiz” path so it can intentionally enter onboarding with `retake=true` instead of being bounced back by the route gate.
- Started web-to-mobile parity audit while the user tests on device.
- Removed mobile spring/springify motion from chat drawers, primary buttons, vibe quiz options, onboarding loading rows, and pricing cards.
- Fixed unreliable Android sidebar scrolling by separating backdrop press targets from drawer content and bounding drawer scroll containers with `flex: 1`.
- Reduced chat list scroll fights by preventing forced auto-scroll while the user is dragging and making AI-burst bottom jumps non-animated.
- Fixed the character detail modal touch shell so scrollable content is no longer nested inside a full-screen backdrop `Pressable`.
- Fixed mobile AI reply persistence: `/api/chat/rendered` now accepts mobile Bearer auth, and mobile falls back to direct Supabase persistence if the deployed API still rejects the rendered-events call.
- Optimized mobile chat rendering after a `VirtualizedList` slow-update warning: precomputed message row metadata, reduced FlatList render batches/window size, memoized chat input/header/list rows, stabilized chat callbacks, and debounced local AsyncStorage message persistence.
- Added a root app error boundary so render crashes show a readable fallback instead of a silent exit when React can catch them.
- Made pricing safe for Expo Go reloads by removing the pricing route's top-level `expo-iap` import and opening the web checkout until Play Billing is verified in an EAS dev/preview build.
- Cleaned the chat top bar density: overlapping character avatars now carry presence, and the old "{n} online · Starter memory" text was removed because it cramped/cut on phone widths.
- Added NativeWind light/dark variables, a persisted Appearance toggle in the chat settings drawer, theme-aware chat wallpaper/bubbles, a right-edge swipe gesture to open settings, and first-load scroll-to-latest behavior for chat.
- Fixed the right-edge swipe target so it no longer overlays the chat header/settings button or input area, and increased chat header icon touch targets to 44px.

## What's broken or unfinished
- User began Expo Go testing and found an onboarding completion loop, springy/buggy chat sidebars, AI replies vanishing after reload, sluggish chat list updates, and Expo Go reload exits; source fixes are in place, but the phone needs a reload/retest.
- If Expo Go still exits instead of showing the error boundary, the next step is to inspect the last 20-30 lines of the Expo terminal because native/module-load crashes can happen before React can catch them.
- Mobile still needs a full real Android phone/dev-client smoke test.
- Parity gaps identified so far: mobile lacks Google OAuth, WYWA rendering, web-style autonomous/idle chat flow, low-message paywall popup/banner, message retry/delivery states, older-history pagination, chat screenshot/share, richer memory-vault affordances, and purchase celebration behavior. Billing/push final QA deferred per user.
- External beta setup remains human/dashboard work: EAS, Play Console, Sentry mobile project, Play Billing credentials, push credentials.
- `.codex/config.toml` was already untracked when I arrived and was left untouched.

## Next session should start with
- Reload Expo Go and verify onboarding now lands in chat, settings/memory drawers scroll smoothly, no “boing” motion remains, AI replies survive app reload, and the `VirtualizedList` slow-update warning disappears or becomes rare.
- Continue the mobile-vs-web parity checklist with failed-message retry, cooldown/paywall UX, WYWA rendering, and older-history pagination.
- Then handle external beta setup: Sentry mobile project, EAS preview build, push credentials, and Play Billing sandbox.

## Decisions made this session
- `docs/superpowers/decisions/0001-expo-go-safe-pricing.md`: pricing opens the web checkout in Expo Go; Play Billing is reconnected from a dev/preview build later.

## Notes
- Verification now passes:
  - `pnpm --filter=@mygang/mobile lint`
  - `pnpm --filter=@mygang/mobile typecheck`
  - `pnpm --filter=@mygang/web test:fast`
  - `pnpm --filter=@mygang/web build`
- After the onboarding loop fix, re-ran mobile typecheck and lint successfully.
- After the sidebar/motion fixes, re-ran mobile typecheck and lint successfully.
- After the AI persistence fix, re-ran mobile typecheck/lint, web fast tests, and web production build successfully.
- After the chat performance pass, re-ran mobile typecheck and lint successfully.
- After the Expo Go reload hardening, re-ran mobile typecheck and lint successfully.
- After the chat header density pass, re-ran mobile typecheck and lint successfully.
- After the theme/drawer/latest-message pass, re-ran mobile typecheck and lint successfully.
- After the swipe-target/header-button fix, re-ran mobile typecheck and lint successfully.
- Full web lint remains blocked by unrelated pre-existing blog/content lint errors (`react/no-unescaped-entities`) and one existing `multi-step-loader` effect rule.
